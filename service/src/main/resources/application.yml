server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:fortune_avenue}
    username: ${DB_USERNAME:fortune_avenue}
    password: ${DB_PASSWORD:fortune_avenue}
  flyway:
    locations: classpath:db/migration
